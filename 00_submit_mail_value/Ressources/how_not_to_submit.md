1. Acceder a la page `ip`/?page=recover
2. Seul un bouton `submit` apparait, clic droit / examiner l'element
3. Supprimer la `value` du champ `mail` (qui est `hidden`)
4. Cliquer sur `Submit` : on obtient le flag.
